/** Blog-specific defaults when env vars are unset (local dev). */
export const APP_DEFAULTS = {
  name: "tgoliveira11-tech",
  slug: "tgoliveira11-tech",
  baseUrl: "http://localhost:3011",
  authCookiePrefixFallback: "postforge-app",
} as const;
